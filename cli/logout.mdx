---
title: "logout"
description: "Clear stored credentials and sign out of the CLI."
---

```bash
memorybase logout
```

Removes your locally stored session credentials. After logging out, you'll need to run `memorybase login` again to use authenticated commands.

## Example

```bash
memorybase logout
```
